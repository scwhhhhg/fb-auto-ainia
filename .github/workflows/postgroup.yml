name: Facebook Auto Post Bot

on:
  # Memungkinkan Anda menjalankan bot secara manual dari tab "Actions"
  workflow_dispatch:
  
  # Menjalankan bot secara otomatis sesuai jadwal
  schedule:
    # Contoh: Jalankan setiap 12 jam sekali
    - cron: '0 */12 * * *'

jobs:
  run-bot:
    runs-on: ubuntu-latest
    
    # Memberikan izin kepada workflow untuk menulis (commit & push) ke repository
    permissions:
      contents: write

    steps:
      - name: 1. Checkout Repository
        uses: actions/checkout@v4
        # Menggunakan token khusus agar bisa melakukan push
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 2. Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 3. Install Dependencies
        run: npm install

      - name: 4. Run the Auto Post Bot
        # Menjalankan skrip postgroup.js
        run: npm run start:postgroup
        env:
          FACEBOOK_COOKIES: ${{ secrets.FACEBOOK_COOKIES }}
          BOT_LICENSE_EMAIL: ${{ secrets.BOT_LICENSE_EMAIL }}

      - name: 5. Commit and Push Log Files
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'

          git pull
          # Menambahkan kedua file log agar selalu tersimpan
          git add posted_groups.txt last_content_index.txt

          # Commit hanya jika ada perubahan yang siap di-commit
          if ! git diff --staged --quiet; then
            git commit -m "Update auto post log files"
            git push
          else
            echo "Tidak ada perubahan pada file log untuk di-commit."
          fi
      
      - name: 6. Upload Debug Screenshots
        # Selalu jalankan langkah ini, meskipun bot gagal, untuk membantu debugging
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-screenshots-postgroup
          path: artifacts/
